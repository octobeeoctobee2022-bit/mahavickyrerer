name: Build Windows EXE

on: [push]

jobs:
  build:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v3

      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Install PyInstaller
        run: pip install pyinstaller

      - name: Build EXE
        run: pyinstaller --onefile amazon_redirect_app.py

      - name: Upload EXE
        uses: actions/upload-artifact@v3
        with:
          name: amazon_redirect_app
          path: dist/amazon_redirect_app.exe
